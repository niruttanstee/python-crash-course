# Constants are variable whose value stays the same throughout the life of a program. Use capital letters to indicate it, such value should never be changed.

MAX_CONNECTIONS = 5000
print(MAX_CONNECTIONS)