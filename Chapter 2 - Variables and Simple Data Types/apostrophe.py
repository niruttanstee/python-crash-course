# Using double quotes correctly or there's a chance of syntax errors.
# This example shows the apostrophe inside the double quote so it's fine.
message = "One of Python's main strengths is its diverse community."
print(message)

# This will cause a syntax error because Python's interpreter can't identify where the string should end.
### message = 'One of Python's main strengths is its diverse community'
